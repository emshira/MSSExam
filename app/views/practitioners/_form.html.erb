<%= form_with(model: practitioner, local: true) do |form| %>
  <% if practitioner.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(practitioner.errors.count, "error") %> prohibited this practitioner from being saved:</h2>

      <ul>
      <% practitioner.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :identification %>
    <%= form.number_field :identification, id: :practitioner_identification %>
  </div>

  <div class="field">
    <%= form.label :last_name %>
    <%= form.text_field :last_name, id: :practitioner_last_name %>
  </div>

  <div class="field">
    <%= form.label :first_name %>
    <%= form.text_field :first_name, id: :practitioner_first_name %>
  </div>

  <div class="field">
    <%= form.label :password_digest %>
    <%= form.text_field :password_digest, id: :practitioner_password_digest %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
