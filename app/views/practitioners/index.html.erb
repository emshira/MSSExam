<p id="notice"><%= notice %></p>

<h1>Practitioners</h1>

<table>
  <thead>
    <tr>
      <th>Identification</th>
      <th>Last name</th>
      <th>First name</th>
      <th>Password digest</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @practitioners.each do |practitioner| %>
      <tr>
        <td><%= practitioner.identification %></td>
        <td><%= practitioner.last_name %></td>
        <td><%= practitioner.first_name %></td>
        <td><%= practitioner.password_digest %></td>
        <td><%= link_to 'Show', practitioner %></td>
        <td><%= link_to 'Edit', edit_practitioner_path(practitioner) %></td>
        <td><%= link_to 'Destroy', practitioner, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Practitioner', new_practitioner_path %>
